name: PR Baseline

on:
  pull_request:
    types:
      - opened
      - edited
      - reopened
      - synchronize
      - ready_for_review

permissions:
  contents: read
  pull-requests: read

jobs:
  pr-baseline-checks:
    uses: ./.github/workflows/reusable-pr-baseline.yml